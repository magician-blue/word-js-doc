{
  "class": {
    "name": "Word.StyleCollection",
    "package": "word",
    "extends": [
      "OfficeExtension.ClientObject"
    ],
    "api_set": {
      "name": "WordApi 1.5",
      "status": null
    },
    "description": "Contains a collection of [Word.Style](https://learn.microsoft.com/en-us/javascript/api/word/word.style) objects.",
    "examples": [
      {
        "description": null,
        "usage_code": "// Link to full sample: // Gets the number of available styles stored with the document.\nawait Word.run(async (context) => {\n  const styles: Word.StyleCollection = context.document.getStyles();\n  const count = styles.getCount();\n  await context.sync();\n\n  console.log(`Number of styles: ${count.value}`);\n});",
        "output_code": null
      }
    ]
  },
  "properties": [
    {
      "name": "context",
      "type": "RequestContext",
      "description": "The request context associated with the object. This connects the add-in's process to the Office host application's process.",
      "since": null,
      "examples": [
        {
          "description": "Access the request context from a StyleCollection to verify the connection between the add-in and Word application is active before performing style operations",
          "usage_code": "await Word.run(async (context) => {\n    const styles = context.document.getStyles();\n    \n    // Access the request context associated with the StyleCollection\n    const requestContext = styles.context;\n    \n    // Verify the context is valid and connected\n    if (requestContext && requestContext.application) {\n        console.log(\"StyleCollection is connected to Word application\");\n        \n        // Load styles using the same context\n        styles.load(\"items\");\n        await context.sync();\n        \n        console.log(`Successfully accessed ${styles.items.length} styles`);\n    }\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "items",
      "type": "Word.Style[]",
      "description": "Gets the loaded child items in this collection.",
      "since": null,
      "examples": [
        {
          "description": "List all available style names in the document to the console",
          "usage_code": "await Word.run(async (context) => {\n    const styles = context.document.getStyles();\n    styles.load(\"items\");\n    \n    await context.sync();\n    \n    console.log(\"Available styles in the document:\");\n    styles.items.forEach((style) => {\n        console.log(`- ${style.nameLocal}`);\n    });\n});",
          "output_code": null
        }
      ]
    }
  ],
  "methods": [
    {
      "name": "getByName",
      "kind": "read",
      "description": "Get the style object by its name.",
      "signatures": [
        {
          "params": [
            {
              "name": "name",
              "type": "string",
              "required": true,
              "description": "The style name."
            }
          ],
          "returns": {
            "type": "Word.Style",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Get the \"Heading 1\" style from the document's style collection and apply it to the first paragraph.",
          "usage_code": "await Word.run(async (context) => {\n    // Get the style collection\n    const styles = context.document.getStyles();\n    \n    // Get the \"Heading 1\" style by name\n    const heading1Style = styles.getByName(\"Heading 1\");\n    \n    // Get the first paragraph in the document\n    const firstParagraph = context.document.body.paragraphs.getFirst();\n    \n    // Load the style name to verify\n    heading1Style.load(\"nameLocal\");\n    \n    await context.sync();\n    \n    // Apply the style to the first paragraph\n    firstParagraph.style = heading1Style.nameLocal;\n    \n    await context.sync();\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "getByNameOrNullObject",
      "kind": "read",
      "description": "If the corresponding style doesn't exist, then this method returns an object with its isNullObject property set to true.",
      "signatures": [
        {
          "params": [
            {
              "name": "name",
              "type": "string",
              "required": true,
              "description": "The style name."
            }
          ],
          "returns": {
            "type": "Word.Style",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Check if a style with the given name already exists in the document, and if not, add a new style with the specified name and type.",
          "usage_code": "// Link to full sample: // Adds a new style.\nawait Word.run(async (context) => {\n  const newStyleName = (document.getElementById(\"new-style-name\") as HTMLInputElement).value;\n  if (newStyleName == \"\") {\n    console.warn(\"Enter a style name to add.\");\n    return;\n  }\n\n  const style: Word.Style = context.document.getStyles().getByNameOrNullObject(newStyleName);\n  style.load();\n  await context.sync();\n\n  if (!style.isNullObject) {\n    console.warn(\n      `There's an existing style with the same name '${newStyleName}'! Please provide another style name.`\n    );\n    return;\n  }\n\n  const newStyleType = ((document.getElementById(\"new-style-type\") as HTMLSelectElement).value as unknown) as Word.StyleType;\n  context.document.addStyle(newStyleName, newStyleType);\n  await context.sync();\n\n  console.log(newStyleName + \" has been added to the style list.\");\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "getCount",
      "kind": "read",
      "description": "Gets the number of the styles in the collection.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "OfficeExtension.ClientResult<number>",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Retrieve and display the total number of styles available in the document.",
          "usage_code": "// Link to full sample: // Gets the number of available styles stored with the document.\nawait Word.run(async (context) => {\n  const styles: Word.StyleCollection = context.document.getStyles();\n  const count = styles.getCount();\n  await context.sync();\n\n  console.log(`Number of styles: ${count.value}`);\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "getItem",
      "kind": "read",
      "description": "Gets a style object by its index in the collection.",
      "signatures": [
        {
          "params": [
            {
              "name": "index",
              "type": "number",
              "required": true,
              "description": "A number that identifies the index location of a style object."
            }
          ],
          "returns": {
            "type": "Word.Style",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Get the third style in the document's style collection and apply it to the first paragraph",
          "usage_code": "await Word.run(async (context) => {\n    // Get the style collection\n    const styles = context.document.getStyles();\n    \n    // Get the style at index 2 (third style)\n    const thirdStyle = styles.getItem(2);\n    thirdStyle.load(\"nameLocal\");\n    \n    // Get the first paragraph\n    const firstParagraph = context.document.body.paragraphs.getFirst();\n    \n    await context.sync();\n    \n    // Apply the style to the paragraph\n    firstParagraph.style = thirdStyle.nameLocal;\n    \n    await context.sync();\n    \n    console.log(`Applied style: ${thirdStyle.nameLocal}`);\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "load",
      "kind": "load",
      "description": "Queues up a command to load the specified properties of the object. You must call context.sync() before reading the properties.",
      "signatures": [
        {
          "params": [
            {
              "name": "options",
              "type": "Word.Interfaces.StyleCollectionLoadOptions & Word.Interfaces.CollectionLoadOptions",
              "required": false,
              "description": "Provides options for which properties of the object to load."
            }
          ],
          "returns": {
            "type": "Word.StyleCollection",
            "description": null
          }
        },
        {
          "params": [
            {
              "name": "propertyNames",
              "type": "string | string[]",
              "required": false,
              "description": "A comma-delimited string or an array of strings that specify the properties to load."
            }
          ],
          "returns": {
            "type": "Word.StyleCollection",
            "description": null
          }
        },
        {
          "params": [
            {
              "name": "propertyNamesAndPaths",
              "type": "OfficeExtension.LoadOption",
              "required": false,
              "description": "propertyNamesAndPaths.select is a comma-delimited string that specifies the properties to load, and propertyNamesAndPaths.expand is a comma-delimited string that specifies the navigation properties to load."
            }
          ],
          "returns": {
            "type": "Word.StyleCollection",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Load and display the names of all available styles in the document",
          "usage_code": "await Word.run(async (context) => {\n    // Get the style collection\n    const styles = context.document.getStyles();\n    \n    // Load the name property for all styles in the collection\n    styles.load(\"items/name\");\n    \n    // Synchronize the document state\n    await context.sync();\n    \n    // Display the style names\n    console.log(\"Available styles:\");\n    styles.items.forEach(style => {\n        console.log(style.name);\n    });\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "toJSON",
      "kind": "serialize",
      "description": "Overrides the JavaScript toJSON() method in order to provide more useful output when an API object is passed to JSON.stringify(). (JSON.stringify, in turn, calls the toJSON method of the object that's passed to it.) Whereas the original Word.StyleCollection object is an API object, the toJSON method returns a plain JavaScript object (typed as Word.Interfaces.StyleCollectionData) that contains an \"items\" array with shallow copies of any loaded properties from the collection's items.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "Word.Interfaces.StyleCollectionData",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Export all available document styles to JSON format for logging or external storage",
          "usage_code": "await Word.run(async (context) => {\n    // Get the style collection from the document\n    const styles = context.document.getStyles();\n    \n    // Load the properties we want to export\n    styles.load(\"items/nameLocal,items/type,items/builtIn\");\n    \n    await context.sync();\n    \n    // Convert the style collection to a plain JavaScript object\n    const stylesJSON = styles.toJSON();\n    \n    // Log or use the JSON data\n    console.log(\"Document Styles:\", JSON.stringify(stylesJSON, null, 2));\n    \n    // Example: Access the items array from the JSON object\n    console.log(`Total styles: ${stylesJSON.items.length}`);\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "track",
      "kind": "track",
      "description": "Track the object for automatic adjustment based on surrounding changes in the document. This call is a shorthand for [context.trackedObjects.add(thisObject)](https://learn.microsoft.com/en-us/javascript/api/office/officeextension.clientrequestcontext#office-officeextension-clientrequestcontext-trackedobjects-member). If you're using this object across .sync calls and outside the sequential execution of a \".run\" batch, and get an \"InvalidObjectPath\" error when setting a property or invoking a method on the object, you need to add the object to the tracked object collection when the object was first created. If this object is part of a collection, you should also track the parent collection.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "Word.StyleCollection",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Track a StyleCollection object to safely access and modify styles across multiple sync calls, preventing \"InvalidObjectPath\" errors when working with the collection outside a single batch operation.",
          "usage_code": "await Word.run(async (context) => {\n    const styles = context.document.getStyles();\n    \n    // Track the StyleCollection to use it across multiple sync calls\n    styles.track();\n    \n    // Load the collection\n    styles.load(\"items\");\n    await context.sync();\n    \n    // First sync - log the count\n    console.log(`Total styles: ${styles.items.length}`);\n    \n    // Perform another operation after sync\n    await context.sync();\n    \n    // Can still safely access the tracked collection\n    for (const style of styles.items) {\n        style.load(\"nameLocal,type\");\n    }\n    await context.sync();\n    \n    // Access properties after multiple syncs\n    styles.items.forEach(style => {\n        console.log(`Style: ${style.nameLocal}, Type: ${style.type}`);\n    });\n    \n    // Untrack when done to free up memory\n    styles.untrack();\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "untrack",
      "kind": "untrack",
      "description": "Release the memory associated with this object, if it has previously been tracked. This call is shorthand for [context.trackedObjects.remove(thisObject)](https://learn.microsoft.com/en-us/javascript/api/office/officeextension.clientrequestcontext#office-officeextension-clientrequestcontext-trackedobjects-member). Having many tracked objects slows down the host application, so please remember to free any objects you add, once you're done using them. You'll need to call context.sync() before the memory release takes effect.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "Word.StyleCollection",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Load and use the styles collection, then release it from memory tracking to improve performance",
          "usage_code": "await Word.run(async (context) => {\n    // Load the styles collection and track it\n    const styles = context.document.getStyles();\n    styles.load(\"items\");\n    \n    await context.sync();\n    \n    // Use the styles collection\n    console.log(`Total styles: ${styles.items.length}`);\n    \n    // Release the memory associated with the styles collection\n    styles.untrack();\n    \n    await context.sync();\n});",
          "output_code": null
        }
      ]
    }
  ],
  "source": {
    "urls": [
      "https://learn.microsoft.com/en-us/javascript/api/word/word.stylecollection"
    ]
  }
}