{
  "class": {
    "name": "Word.InlinePictureCollection",
    "package": "word",
    "extends": [
      "OfficeExtension.ClientObject"
    ],
    "api_set": {
      "name": "WordApi 1.1",
      "status": null
    },
    "description": "Contains a collection of [Word.InlinePicture](/en-us/javascript/api/word/word.inlinepicture) objects.",
    "examples": [
      {
        "description": null,
        "usage_code": "// Link to full sample: https://raw.githubusercontent.com/OfficeDev/office-js-snippets/prod/samples/word/15-images/insert-and-get-pictures.yaml\n\n// Gets the first image in the document.\nawait Word.run(async (context) => {\n  const firstPicture: Word.InlinePicture = context.document.body.inlinePictures.getFirst();\n  firstPicture.load(\"width, height, imageFormat\");\n\n  await context.sync();\n  console.log(`Image dimensions: ${firstPicture.width} x ${firstPicture.height}`, `Image format: ${firstPicture.imageFormat}`);\n  // Get the image encoded as Base64.\n  const base64 = firstPicture.getBase64ImageSrc();\n\n  await context.sync();\n  console.log(base64.value);\n});",
        "output_code": null
      }
    ]
  },
  "properties": [
    {
      "name": "context",
      "type": "RequestContext",
      "description": "The request context associated with the object. This connects the add-in's process to the Office host application's process.",
      "since": null,
      "examples": [
        {
          "description": "Access the request context from an InlinePictureCollection to verify the connection between the add-in and Word application before performing operations on inline pictures.",
          "usage_code": "await Word.run(async (context) => {\n    const inlinePictures = context.document.body.inlinePictures;\n    \n    // Access the request context associated with the collection\n    const requestContext = inlinePictures.context;\n    \n    // Verify the context is valid by using it to load properties\n    inlinePictures.load(\"items\");\n    await requestContext.sync();\n    \n    console.log(`Connected to Word. Found ${inlinePictures.items.length} inline pictures.`);\n    console.log(`Request context type: ${requestContext.constructor.name}`);\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "items",
      "type": "Word.InlinePicture[]",
      "description": "Gets the loaded child items in this collection.",
      "since": null,
      "examples": [
        {
          "description": "Get all inline pictures in the document and log their count and dimensions to the console.",
          "usage_code": "await Word.run(async (context) => {\n    // Get all inline pictures in the document body\n    const inlinePictures = context.document.body.inlinePictures;\n    \n    // Load the items array with width and height properties\n    inlinePictures.load(\"items\");\n    \n    await context.sync();\n    \n    // Access the loaded items array\n    const pictureItems = inlinePictures.items;\n    \n    console.log(`Found ${pictureItems.length} inline pictures`);\n    \n    // Iterate through each picture in the items array\n    pictureItems.forEach((picture, index) => {\n        picture.load(\"width, height\");\n    });\n    \n    await context.sync();\n    \n    pictureItems.forEach((picture, index) => {\n        console.log(`Picture ${index + 1}: ${picture.width}x${picture.height} pixels`);\n    });\n});",
          "output_code": null
        }
      ]
    }
  ],
  "methods": [
    {
      "name": "getFirst",
      "kind": "read",
      "description": "Gets the first inline image in this collection. Throws an ItemNotFound error if this collection is empty.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "Word.InlinePicture",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Get the first inline picture in the document and resize it to 150x150 pixels",
          "usage_code": "await Word.run(async (context) => {\n    // Get all inline pictures in the document body\n    const inlinePictures = context.document.body.inlinePictures;\n    \n    // Get the first inline picture\n    const firstPicture = inlinePictures.getFirst();\n    \n    // Resize the picture\n    firstPicture.width = 150;\n    firstPicture.height = 150;\n    \n    await context.sync();\n    \n    console.log(\"First inline picture resized successfully\");\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "getFirstOrNullObject",
      "kind": "read",
      "description": "Gets the first inline image in this collection. If this collection is empty, then this method will return an object with its isNullObject property set to true. For further information, see [*OrNullObject methods and properties](/en-us/office/dev/add-ins/develop/application-specific-api-model#ornullobject-methods-and-properties).",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "Word.InlinePicture",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Check if a document contains any inline images and display an alert with the first image's width, or notify the user if no images exist.",
          "usage_code": "await Word.run(async (context) => {\n    const inlinePictures = context.document.body.inlinePictures;\n    const firstPicture = inlinePictures.getFirstOrNullObject();\n    firstPicture.load(\"width, isNullObject\");\n    \n    await context.sync();\n    \n    if (firstPicture.isNullObject) {\n        console.log(\"No inline images found in the document.\");\n    } else {\n        console.log(`First inline image width: ${firstPicture.width} pixels`);\n    }\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "load",
      "kind": "load",
      "description": "Queues up a command to load the specified properties of the object. You must call context.sync() before reading the properties.",
      "signatures": [
        {
          "params": [
            {
              "name": "options",
              "type": "Word.Interfaces.InlinePictureCollectionLoadOptions & Word.Interfaces.CollectionLoadOptions",
              "required": false,
              "description": "Provides options for which properties of the object to load."
            }
          ],
          "returns": {
            "type": "Word.InlinePictureCollection",
            "description": null
          }
        },
        {
          "params": [
            {
              "name": "propertyNames",
              "type": "string | string[]",
              "required": false,
              "description": "A comma-delimited string or an array of strings that specify the properties to load."
            }
          ],
          "returns": {
            "type": "Word.InlinePictureCollection",
            "description": null
          }
        },
        {
          "params": [
            {
              "name": "propertyNamesAndPaths",
              "type": "OfficeExtension.LoadOption",
              "required": false,
              "description": "propertyNamesAndPaths.select is a comma-delimited string that specifies the properties to load, and propertyNamesAndPaths.expand is a comma-delimited string that specifies the navigation properties to load."
            }
          ],
          "returns": {
            "type": "Word.InlinePictureCollection",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Load and display the width and height properties of all inline pictures in the document",
          "usage_code": "await Word.run(async (context) => {\n    // Get all inline pictures in the document\n    const inlinePictures = context.document.body.inlinePictures;\n    \n    // Load width and height properties for all inline pictures\n    inlinePictures.load(\"width, height\");\n    \n    await context.sync();\n    \n    // Display the properties\n    for (let i = 0; i < inlinePictures.items.length; i++) {\n        console.log(`Picture ${i + 1}: Width = ${inlinePictures.items[i].width}, Height = ${inlinePictures.items[i].height}`);\n    }\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "toJSON",
      "kind": "serialize",
      "description": "Overrides the JavaScript toJSON() method in order to provide more useful output when an API object is passed to JSON.stringify(). (JSON.stringify, in turn, calls the toJSON method of the object that's passed to it.) Whereas the original Word.InlinePictureCollection object is an API object, the toJSON method returns a plain JavaScript object (typed as Word.Interfaces.InlinePictureCollectionData) that contains an \"items\" array with shallow copies of any loaded properties from the collection's items.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "Word.Interfaces.InlinePictureCollectionData",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Export inline picture collection data to JSON format for logging or external storage",
          "usage_code": "await Word.run(async (context) => {\n    // Get all inline pictures in the document body\n    const inlinePictures = context.document.body.inlinePictures;\n    \n    // Load properties we want to include in the JSON output\n    inlinePictures.load(\"width,height,altTextTitle,altTextDescription\");\n    \n    await context.sync();\n    \n    // Convert the collection to a plain JavaScript object\n    const picturesData = inlinePictures.toJSON();\n    \n    // Log or use the JSON data\n    console.log(\"Inline Pictures Data:\", JSON.stringify(picturesData, null, 2));\n    console.log(`Total pictures found: ${picturesData.items.length}`);\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "track",
      "kind": "track",
      "description": "Track the object for automatic adjustment based on surrounding changes in the document. This call is a shorthand for [context.trackedObjects.add(thisObject)](/en-us/javascript/api/office/officeextension.clientrequestcontext#office-officeextension-clientrequestcontext-trackedobjects-member). If you're using this object across .sync calls and outside the sequential execution of a \".run\" batch, and get an \"InvalidObjectPath\" error when setting a property or invoking a method on the object, you need to add the object to the tracked object collection when the object was first created. If this object is part of a collection, you should also track the parent collection.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "Word.InlinePictureCollection",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Track inline pictures in a document to maintain references across multiple sync calls while modifying their properties",
          "usage_code": "await Word.run(async (context) => {\n    // Get all inline pictures in the document\n    const inlinePictures = context.document.body.inlinePictures;\n    inlinePictures.load(\"items\");\n    await context.sync();\n\n    // Track the collection to use it across multiple sync calls\n    inlinePictures.track();\n\n    // First sync - modify width\n    await context.sync();\n    \n    if (inlinePictures.items.length > 0) {\n        inlinePictures.items[0].width = 200;\n    }\n\n    // Second sync - modify height\n    await context.sync();\n    \n    if (inlinePictures.items.length > 0) {\n        inlinePictures.items[0].height = 150;\n    }\n\n    await context.sync();\n\n    // Untrack when done to free up memory\n    inlinePictures.untrack();\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "untrack",
      "kind": "untrack",
      "description": "Release the memory associated with this object, if it has previously been tracked. This call is shorthand for [context.trackedObjects.remove(thisObject)](/en-us/javascript/api/office/officeextension.clientrequestcontext#office-officeextension-clientrequestcontext-trackedobjects-member). Having many tracked objects slows down the host application, so please remember to free any objects you add, once you're done using them. You'll need to call context.sync() before the memory release takes effect.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "Word.InlinePictureCollection",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Load inline pictures from the document, process them, then untrack the collection to free memory after use.",
          "usage_code": "await Word.run(async (context) => {\n    // Get all inline pictures in the document body\n    const inlinePictures = context.document.body.inlinePictures;\n    \n    // Track the collection for processing\n    inlinePictures.load(\"items\");\n    await context.sync();\n    \n    // Process the pictures (e.g., log count)\n    console.log(`Found ${inlinePictures.items.length} inline pictures`);\n    \n    // Untrack the collection to release memory\n    inlinePictures.untrack();\n    await context.sync();\n});",
          "output_code": null
        }
      ]
    }
  ],
  "source": {
    "urls": [
      "https://docs.microsoft.com/en-us/javascript/api/word/word.inlinepicturecollection"
    ]
  }
}