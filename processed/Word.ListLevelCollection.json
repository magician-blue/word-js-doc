{
  "class": {
    "name": "Word.ListLevelCollection",
    "package": "word",
    "extends": [
      "OfficeExtension.ClientObject"
    ],
    "api_set": {
      "name": "WordApiDesktop",
      "status": "1.1"
    },
    "description": "Contains a collection of [Word.ListLevel](/en-us/javascript/api/word/word.listlevel) objects.",
    "examples": [
      {
        "description": null,
        "usage_code": "// Link to full sample: https://raw.githubusercontent.com/OfficeDev/office-js-snippets/prod/samples/word/20-lists/manage-list-styles.yaml\n\n// Gets the properties of the specified style.\nawait Word.run(async (context) => {\n  const styleName = (document.getElementById(\"style-name-to-use\") as HTMLInputElement).value;\n  if (styleName == \"\") {\n    console.warn(\"Enter a style name to get properties.\");\n    return;\n  }\n\n  const style: Word.Style = context.document.getStyles().getByNameOrNullObject(styleName);\n  style.load(\"type\");\n  await context.sync();\n\n  if (style.isNullObject || style.type != Word.StyleType.list) {\n    console.warn(`There's no existing style with the name '${styleName}'. Or this isn't a list style.`);\n  } else {\n    // Load objects to log properties and their values in the console.\n    style.load();\n    style.listTemplate.load();\n    await context.sync();\n\n    console.log(`Properties of the '${styleName}' style:`, style);\n\n    const listLevels = style.listTemplate.listLevels;\n    listLevels.load(\"items\");\n    await context.sync();\n\n    console.log(`List levels of the '${styleName}' style:`, listLevels);\n  }\n});",
        "output_code": null
      }
    ]
  },
  "properties": [
    {
      "name": "context",
      "type": "RequestContext",
      "description": "The request context associated with the object. This connects the add-in's process to the Office host application's process.",
      "since": null,
      "examples": [
        {
          "description": "Access the request context from a ListLevelCollection to verify the connection to the Word host application and log its diagnostic information.",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first list in the document\n    const lists = context.document.body.lists;\n    lists.load(\"items\");\n    await context.sync();\n    \n    if (lists.items.length > 0) {\n        const firstList = lists.items[0];\n        const listLevels = firstList.levelTypes;\n        \n        // Access the request context from the ListLevelCollection\n        const requestContext = listLevels.context;\n        \n        // Verify the context is valid and connected\n        console.log(\"Request context is connected:\", requestContext !== null);\n        console.log(\"Context debug info:\", requestContext.debugInfo);\n        \n        await context.sync();\n    }\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "items",
      "type": "Word.ListLevel[]",
      "description": "Gets the loaded child items in this collection.",
      "since": null,
      "examples": [
        {
          "description": "Iterate through all list levels in a list and display the level number and indentation for each level.",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first list in the document\n    const lists = context.document.body.lists;\n    lists.load(\"items\");\n    await context.sync();\n    \n    if (lists.items.length > 0) {\n        const firstList = lists.items[0];\n        const listLevels = firstList.levelTypes;\n        listLevels.load(\"items\");\n        await context.sync();\n        \n        // Access the items property to get all loaded list levels\n        const levels = listLevels.items;\n        \n        console.log(`Found ${levels.length} list levels:`);\n        levels.forEach((level, index) => {\n            level.load(\"alignment\");\n            console.log(`Level ${index}: ${level.alignment}`);\n        });\n        \n        await context.sync();\n    }\n});",
          "output_code": null
        }
      ]
    }
  ],
  "methods": [
    {
      "name": "getFirst",
      "kind": "read",
      "description": "Gets the first list level in this collection. Throws an ItemNotFound error if this collection is empty.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "Word.ListLevel",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Get the first list level from a numbered list and change its alignment to right",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first list in the document\n    const lists = context.document.body.lists;\n    lists.load(\"items\");\n    await context.sync();\n    \n    if (lists.items.length > 0) {\n        const firstList = lists.items[0];\n        \n        // Get the list level collection\n        const listLevels = firstList.levelTypes[0].listLevels;\n        \n        // Get the first list level\n        const firstLevel = listLevels.getFirst();\n        firstLevel.load(\"alignment\");\n        await context.sync();\n        \n        // Change the alignment to right\n        firstLevel.alignment = Word.Alignment.right;\n        await context.sync();\n        \n        console.log(\"First list level alignment changed to right\");\n    }\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "getFirstOrNullObject",
      "kind": "read",
      "description": "Gets the first list level in this collection. If this collection is empty, then this method will return an object with its isNullObject property set to true. For further information, see [*OrNullObject methods and properties](/en-us/office/dev/add-ins/develop/application-specific-api-model#ornullobject-methods-and-properties).",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "Word.ListLevel",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Check if a list has any levels defined and display the alignment of the first level if it exists.",
          "usage_code": "await Word.run(async (context) => {\n    const list = context.document.body.lists.getFirst();\n    const firstLevel = list.listLevels.getFirstOrNullObject();\n    firstLevel.load(\"isNullObject, alignment\");\n    \n    await context.sync();\n    \n    if (firstLevel.isNullObject) {\n        console.log(\"This list has no levels defined.\");\n    } else {\n        console.log(\"First level alignment: \" + firstLevel.alignment);\n    }\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "load",
      "kind": "load",
      "description": "Queues up a command to load the specified properties of the object. You must call context.sync() before reading the properties.",
      "signatures": [
        {
          "params": [
            {
              "name": "options",
              "type": "Word.Interfaces.ListLevelCollectionLoadOptions & Word.Interfaces.CollectionLoadOptions",
              "required": false,
              "description": "Provides options for which properties of the object to load."
            }
          ],
          "returns": {
            "type": "Word.ListLevelCollection",
            "description": null
          }
        },
        {
          "params": [
            {
              "name": "propertyNames",
              "type": "string | string[]",
              "required": false,
              "description": "A comma-delimited string or an array of strings that specify the properties to load."
            }
          ],
          "returns": {
            "type": "Word.ListLevelCollection",
            "description": null
          }
        },
        {
          "params": [
            {
              "name": "propertyNamesAndPaths",
              "type": "OfficeExtension.LoadOption",
              "required": false,
              "description": "propertyNamesAndPaths.select is a comma-delimited string that specifies the properties to load, and propertyNamesAndPaths.expand is a comma-delimited string that specifies the navigation properties to load."
            }
          ],
          "returns": {
            "type": "Word.ListLevelCollection",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Load and display the alignment property of all list levels in the first list of the document",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first list in the document\n    const list = context.document.body.lists.getFirst();\n    \n    // Get the list level collection\n    const listLevels = list.levelTypes.getFirst().listLevels;\n    \n    // Load the alignment property for all list levels\n    listLevels.load(\"items/alignment\");\n    \n    await context.sync();\n    \n    // Display the alignment of each list level\n    listLevels.items.forEach((level, index) => {\n        console.log(`Level ${index}: ${level.alignment}`);\n    });\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "toJSON",
      "kind": "serialize",
      "description": "Overrides the JavaScript toJSON() method in order to provide more useful output when an API object is passed to JSON.stringify(). (JSON.stringify, in turn, calls the toJSON method of the object that's passed to it.) Whereas the original Word.ListLevelCollection object is an API object, the toJSON method returns a plain JavaScript object (typed as Word.Interfaces.ListLevelCollectionData) that contains an \"items\" array with shallow copies of any loaded properties from the collection's items.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "Word.Interfaces.ListLevelCollectionData",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Retrieve list level information from the first list in a document and serialize it to JSON for logging or data export purposes.",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first list in the document\n    const list = context.document.body.lists.getFirst();\n    \n    // Get the list level collection\n    const listLevels = list.levelTypes;\n    \n    // Load the properties we want to serialize\n    listLevels.load(\"items\");\n    \n    await context.sync();\n    \n    // Convert the collection to a plain JavaScript object\n    const listLevelsData = listLevels.toJSON();\n    \n    // Now you can work with the plain object (e.g., log it, send to server, etc.)\n    console.log(\"List levels data:\", JSON.stringify(listLevelsData, null, 2));\n    console.log(\"Number of list levels:\", listLevelsData.items.length);\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "track",
      "kind": "track",
      "description": "Track the object for automatic adjustment based on surrounding changes in the document. This call is a shorthand for [context.trackedObjects.add(thisObject)](/en-us/javascript/api/office/officeextension.clientrequestcontext#office-officeextension-clientrequestcontext-trackedobjects-member). If you're using this object across .sync calls and outside the sequential execution of a \".run\" batch, and get an \"InvalidObjectPath\" error when setting a property or invoking a method on the object, you need to add the object to the tracked object collection when the object was first created. If this object is part of a collection, you should also track the parent collection.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "Word.ListLevelCollection",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Track a list level collection object to maintain its reference across multiple sync calls when analyzing list formatting properties",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first list in the document\n    const lists = context.document.body.lists;\n    lists.load(\"items\");\n    await context.sync();\n    \n    if (lists.items.length > 0) {\n        const firstList = lists.items[0];\n        \n        // Get the list level collection\n        const listLevels = firstList.listLevels;\n        \n        // Track the collection to use it across multiple sync calls\n        listLevels.track();\n        \n        // Load properties\n        listLevels.load(\"items\");\n        await context.sync();\n        \n        // First sync - log count\n        console.log(`Number of list levels: ${listLevels.items.length}`);\n        await context.sync();\n        \n        // Second sync - access the tracked object again\n        console.log(`Still accessible: ${listLevels.items.length} levels`);\n        \n        // Untrack when done\n        listLevels.untrack();\n    }\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "untrack",
      "kind": "untrack",
      "description": "Release the memory associated with this object, if it has previously been tracked. This call is shorthand for [context.trackedObjects.remove(thisObject)](/en-us/javascript/api/office/officeextension.clientrequestcontext#office-officeextension-clientrequestcontext-trackedobjects-member). Having many tracked objects slows down the host application, so please remember to free any objects you add, once you're done using them. You'll need to call context.sync() before the memory release takes effect.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "Word.ListLevelCollection",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Access list level collection properties, then untrack the object to free memory after use",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first list in the document\n    const list = context.document.body.lists.getFirst();\n    const listLevelCollection = list.levelTypes;\n    \n    // Track the collection to work with it\n    listLevelCollection.track();\n    \n    // Load properties to use the collection\n    listLevelCollection.load(\"items\");\n    await context.sync();\n    \n    // Use the collection (e.g., log the count)\n    console.log(`Number of list levels: ${listLevelCollection.items.length}`);\n    \n    // Untrack the object to release memory\n    listLevelCollection.untrack();\n    await context.sync();\n});",
          "output_code": null
        }
      ]
    }
  ],
  "source": {
    "urls": [
      "/en-us/javascript/api/word",
      "https://raw.githubusercontent.com/OfficeDev/office-js-snippets/prod/samples/word/20-lists/manage-list-styles.yaml"
    ]
  }
}