{
  "class": {
    "name": "Word.Page",
    "package": "https://learn.microsoft.com/en-us/javascript/api/word",
    "extends": [
      "https://learn.microsoft.com/en-us/javascript/api/office/officeextension.clientobject"
    ],
    "api_set": {
      "name": "WordApiDesktop",
      "status": "1.2"
    },
    "description": "Represents a page in the document. Page objects manage the page layout and content.",
    "examples": [
      {
        "description": null,
        "usage_code": "// Link to full sample: https://raw.githubusercontent.com/OfficeDev/office-js-snippets/prod/samples/word/35-ranges/get-pages.yaml\n\nawait Word.run(async (context) => {\n  // Gets pages of the selection.\n  const pages: Word.PageCollection = context.document.getSelection().pages;\n  pages.load();\n  await context.sync();\n\n  // Log info for pages included in selection.\n  console.log(pages);\n  const pagesIndexes = [];\n  const pagesText = [];\n  for (let i = 0; i < pages.items.length; i++) {\n    const page = pages.items[i];\n    page.load('index');\n    pagesIndexes.push(page);\n\n    const range = page.getRange();\n    range.load('text');\n    pagesText.push(range);\n  }\n\n  await context.sync();\n\n  for (let i = 0; i < pagesIndexes.length; i++) {\n    console.log(`Index info for page ${i + 1} in the selection: ${pagesIndexes[i].index}`);\n    console.log(\"Text of that page in the selection:\", pagesText[i].text);\n  }\n});",
        "output_code": null
      }
    ]
  },
  "properties": [
    {
      "name": "breaks",
      "type": null,
      "description": "Gets a BreakCollection object that represents the breaks on the page.",
      "since": null,
      "examples": [
        {
          "description": "Get all page breaks on the first page and display their count and types in the console.",
          "usage_code": "await Word.run(async (context) => {\n    const firstPage = context.document.body.sections.getFirst().getFirstPage();\n    const breaks = firstPage.breaks;\n    \n    breaks.load(\"items/type\");\n    await context.sync();\n    \n    console.log(`Total breaks on page: ${breaks.items.length}`);\n    breaks.items.forEach((pageBreak, index) => {\n        console.log(`Break ${index + 1}: ${pageBreak.type}`);\n    });\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "context",
      "type": null,
      "description": "The request context associated with the object. This connects the add-in's process to the Office host application's process.",
      "since": null,
      "examples": [
        {
          "description": "Access the page's request context to verify the connection between the add-in and Word before performing page operations",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first page in the document\n    const page = context.document.body.getRange().getPage();\n    \n    // Access the request context associated with the page object\n    const pageContext = page.context;\n    \n    // Verify the context is valid by using it to load page properties\n    page.load(\"isLastPage\");\n    \n    await pageContext.sync();\n    \n    console.log(\"Page context is active and connected to Word\");\n    console.log(`Is last page: ${page.isLastPage}`);\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "height",
      "type": null,
      "description": "Gets the height, in points, of the paper defined in the Page Setup dialog box.",
      "since": null,
      "examples": [
        {
          "description": "Display the current page height in points to the user in a message dialog",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first page of the document\n    const page = context.document.body.pages.getFirst();\n    \n    // Load the height property\n    page.load(\"height\");\n    \n    await context.sync();\n    \n    // Display the page height\n    console.log(`Page height: ${page.height} points`);\n    // Or show in a dialog: OfficeExtension.UI.displayDialogAsync(`Page height: ${page.height} points`);\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "index",
      "type": null,
      "description": "Gets the index of the page. The page index is 1-based and independent of the user's custom page numbering.",
      "since": null,
      "examples": [
        {
          "description": "Display the actual page index of the first page in the document to verify it starts at 1",
          "usage_code": "await Word.run(async (context) => {\n    const firstPage = context.document.body.paragraphs.getFirst().getRange().getPage();\n    firstPage.load(\"index\");\n    \n    await context.sync();\n    \n    console.log(`The first page index is: ${firstPage.index}`);\n    // Output: The first page index is: 1\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "width",
      "type": null,
      "description": "Gets the width, in points, of the paper defined in the Page Setup dialog box.",
      "since": null,
      "examples": [
        {
          "description": "Display the current page width in points to verify the paper size settings",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first page of the document\n    const page = context.document.body.sections.getFirst().getFirstPageOrNullObject();\n    \n    // Load the width property\n    page.load(\"width\");\n    \n    await context.sync();\n    \n    // Display the page width\n    console.log(`Page width: ${page.width} points`);\n});",
          "output_code": null
        }
      ]
    }
  ],
  "methods": [
    {
      "name": "getNext",
      "kind": "read",
      "description": "Gets the next page in the pane. Throws an ItemNotFound error if this page is the last one.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": null,
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Get the next page after the first page and highlight all text on that second page with yellow background color.",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first page in the document\n    const firstPage = context.document.body.pages.getFirst();\n    \n    // Get the next page (second page)\n    const secondPage = firstPage.getNext();\n    \n    // Load the body of the second page\n    secondPage.load(\"body\");\n    \n    await context.sync();\n    \n    // Highlight all text on the second page with yellow\n    secondPage.body.font.highlightColor = \"yellow\";\n    \n    await context.sync();\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "getNextOrNullObject",
      "kind": "read",
      "description": "Gets the next page. If this page is the last one, then this method will return an object with its isNullObject property set to true. For further information, see *OrNullObject methods and properties.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": null,
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Check if the current page has a next page and display an alert indicating whether it's the last page in the document.",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first page in the document\n    const firstPage = context.document.body.paragraphs.getFirst().getRange().getPage();\n    \n    // Get the next page or null object\n    const nextPage = firstPage.getNextOrNullObject();\n    \n    // Load the isNullObject property\n    nextPage.load(\"isNullObject\");\n    \n    await context.sync();\n    \n    // Check if there is a next page\n    if (nextPage.isNullObject) {\n        console.log(\"This is the last page in the document.\");\n    } else {\n        console.log(\"There is a next page after the current page.\");\n    }\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "getRange",
      "kind": "read",
      "description": "Gets the whole page, or the starting or ending point of the page, as a range.",
      "signatures": [
        {
          "params": [
            {
              "name": "rangeLocation",
              "type": null,
              "required": true,
              "description": null
            }
          ],
          "returns": {
            "type": null,
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Highlight the entire content of the first page by getting its range and applying a yellow background color",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first page in the document\n    const firstPage = context.document.body.sections.getFirst().getFirstPageHeader().page;\n    \n    // Alternative: Get pages from body\n    const pages = context.document.body.getRange().getPages();\n    const page = pages.items[0];\n    \n    // Get the range for the entire page\n    const pageRange = page.getRange(\"Whole\");\n    \n    // Apply yellow highlighting to the entire page content\n    pageRange.font.highlightColor = \"Yellow\";\n    \n    await context.sync();\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "load",
      "kind": "load",
      "description": "Queues up a command to load the specified properties of the object. You must call context.sync() before reading the properties.",
      "signatures": [
        {
          "params": [
            {
              "name": "options",
              "type": null,
              "required": true,
              "description": null
            }
          ],
          "returns": {
            "type": null,
            "description": null
          }
        },
        {
          "params": [
            {
              "name": "propertyNames",
              "type": null,
              "required": true,
              "description": null
            }
          ],
          "returns": {
            "type": null,
            "description": null
          }
        },
        {
          "params": [
            {
              "name": "propertyNamesAndPaths",
              "type": null,
              "required": true,
              "description": null
            }
          ],
          "returns": {
            "type": null,
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Load and display the page number and dimensions of the first page in the document",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first page in the document\n    const firstPage = context.document.body.pages.getFirst();\n    \n    // Load specific properties of the page\n    firstPage.load(\"pageNumber, width, height\");\n    \n    // Sync to execute the load command\n    await context.sync();\n    \n    // Access the loaded properties\n    console.log(`Page Number: ${firstPage.pageNumber}`);\n    console.log(`Width: ${firstPage.width} points`);\n    console.log(`Height: ${firstPage.height} points`);\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "toJSON",
      "kind": "serialize",
      "description": "Overrides the JavaScript toJSON() method in order to provide more useful output when an API object is passed to JSON.stringify(). (JSON.stringify, in turn, calls the toJSON method of the object that's passed to it.) Whereas the original Word.Page object is an API object, the toJSON method returns a plain JavaScript object (typed as Word.Interfaces.PageData) that contains shallow copies of any loaded child properties from the original object.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": null,
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Serialize a page object to JSON format to log or store its properties for debugging purposes",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first page in the document\n    const page = context.document.body.pages.getFirst();\n    \n    // Load properties we want to serialize\n    page.load(\"pageIndex\");\n    \n    await context.sync();\n    \n    // Convert the page object to a plain JavaScript object\n    const pageData = page.toJSON();\n    \n    // Now you can use the plain object (e.g., log it, store it, etc.)\n    console.log(\"Page data:\", JSON.stringify(pageData, null, 2));\n    console.log(\"Page index:\", pageData.pageIndex);\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "track",
      "kind": "track",
      "description": "Track the object for automatic adjustment based on surrounding changes in the document. This call is a shorthand for context.trackedObjects.add(thisObject). If you're using this object across .sync calls and outside the sequential execution of a \".run\" batch, and get an \"InvalidObjectPath\" error when setting a property or invoking a method on the object, you need to add the object to the tracked object collection when the object was first created. If this object is part of a collection, you should also track the parent collection.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": null,
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Track a page object to maintain its reference across multiple sync calls when checking and updating page properties",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first page\n    const page = context.document.body.pages.getFirst();\n    \n    // Track the page object to use it across multiple sync calls\n    page.track();\n    \n    // Load properties\n    page.load(\"pageIndex\");\n    await context.sync();\n    \n    console.log(`Current page index: ${page.pageIndex}`);\n    \n    // Perform additional operations after sync\n    // The tracked object remains valid\n    page.load(\"height,width\");\n    await context.sync();\n    \n    console.log(`Page dimensions: ${page.width} x ${page.height}`);\n    \n    // Untrack when done to free up memory\n    page.untrack();\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "untrack",
      "kind": "untrack",
      "description": "Release the memory associated with this object, if it has previously been tracked. This call is a shorthand for context.trackedObjects.remove(thisObject).",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": null,
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Load page properties, use them for processing, then untrack the page object to free memory when done",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first page and track it\n    const page = context.document.body.pages.getFirst();\n    page.track();\n    \n    // Load properties for use\n    page.load(\"width,height\");\n    await context.sync();\n    \n    // Use the page properties\n    console.log(`Page dimensions: ${page.width} x ${page.height}`);\n    \n    // Untrack the page object to release memory\n    page.untrack();\n    \n    await context.sync();\n});",
          "output_code": null
        }
      ]
    }
  ],
  "source": {
    "urls": [
      "https://learn.microsoft.com/en-us/javascript/api/word",
      "https://learn.microsoft.com/en-us/javascript/api/office/officeextension.clientobject"
    ]
  }
}