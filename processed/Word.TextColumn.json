{
  "class": {
    "name": "Word.TextColumn",
    "package": "word",
    "extends": [
      "OfficeExtension.ClientObject"
    ],
    "api_set": {
      "name": "WordApi",
      "status": "BETA (PREVIEW ONLY)"
    },
    "description": "Represents a single text column in a section.",
    "examples": []
  },
  "properties": [
    {
      "name": "context",
      "type": "RequestContext",
      "description": "The request context associated with the object. This connects the add-in's process to the Office host application's process.",
      "since": null,
      "examples": [
        {
          "description": "Access the request context from a TextColumn object to verify the connection between the add-in and Word application before performing operations on the column.",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first section and its text columns\n    const section = context.document.sections.getFirst();\n    const textColumns = section.textColumns;\n    textColumns.load(\"items\");\n    \n    await context.sync();\n    \n    if (textColumns.items.length > 0) {\n        const firstColumn = textColumns.items[0];\n        \n        // Access the request context from the TextColumn object\n        const columnContext = firstColumn.context;\n        \n        // Verify the context is valid and connected\n        console.log(\"TextColumn context is connected:\", columnContext !== null);\n        console.log(\"Context type:\", typeof columnContext);\n        \n        // Use the context to perform operations\n        firstColumn.load(\"width\");\n        await columnContext.sync();\n        \n        console.log(\"Column width:\", firstColumn.width);\n    }\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "spaceAfter",
      "type": "number",
      "description": "Specifies the amount of spacing (in points) after the specified paragraph or text column.",
      "since": "WordApi BETA (PREVIEW ONLY)",
      "examples": [
        {
          "description": "Set the spacing after a text column to 24 points to add visual separation between columns",
          "usage_code": "await Word.run(async (context) => {\n    const section = context.document.sections.getFirst();\n    const textColumns = section.textColumns;\n    textColumns.load(\"items\");\n    \n    await context.sync();\n    \n    // Set 24 points of spacing after the first text column\n    if (textColumns.items.length > 0) {\n        textColumns.items[0].spaceAfter = 24;\n    }\n    \n    await context.sync();\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "width",
      "type": "number",
      "description": "Specifies the width, in points, of the specified text columns.",
      "since": "WordApi BETA (PREVIEW ONLY)",
      "examples": [
        {
          "description": "Set the width of the first text column in the active document's first section to 200 points.",
          "usage_code": "await Word.run(async (context) => {\n    const section = context.document.sections.getFirst();\n    const textColumns = section.body.textColumns;\n    textColumns.load(\"items\");\n    \n    await context.sync();\n    \n    if (textColumns.items.length > 0) {\n        const firstColumn = textColumns.items[0];\n        firstColumn.width = 200;\n        \n        await context.sync();\n    }\n});",
          "output_code": null
        }
      ]
    }
  ],
  "methods": [
    {
      "name": "load",
      "kind": "load",
      "description": "Queues up a command to load the specified properties of the object. You must call context.sync() before reading the properties.",
      "signatures": [
        {
          "params": [
            {
              "name": "options",
              "type": "Word.Interfaces.TextColumnLoadOptions",
              "required": false,
              "description": "Provides options for which properties of the object to load."
            }
          ],
          "returns": {
            "type": "Word.TextColumn",
            "description": null
          }
        },
        {
          "params": [
            {
              "name": "propertyNames",
              "type": "string | string[]",
              "required": false,
              "description": "A comma-delimited string or an array of strings that specify the properties to load."
            }
          ],
          "returns": {
            "type": "Word.TextColumn",
            "description": null
          }
        },
        {
          "params": [
            {
              "name": "propertyNamesAndPaths",
              "type": "{ select?: string; expand?: string; }",
              "required": false,
              "description": "propertyNamesAndPaths.select is a comma-delimited string that specifies the properties to load, and propertyNamesAndPaths.expand is a comma-delimited string that specifies the navigation properties to load."
            }
          ],
          "returns": {
            "type": "Word.TextColumn",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Read and display the width and spacing properties of the first text column in the active document's first section.",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first section and its text columns\n    const section = context.document.sections.getFirst();\n    const textColumns = section.body.textColumns;\n    const firstColumn = textColumns.getFirst();\n    \n    // Load properties of the first text column\n    firstColumn.load(\"width, spaceAfter\");\n    \n    await context.sync();\n    \n    // Display the loaded properties\n    console.log(`Column width: ${firstColumn.width}`);\n    console.log(`Space after column: ${firstColumn.spaceAfter}`);\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "set",
      "kind": "write",
      "description": "Sets multiple properties of an object at the same time. You can pass either a plain object with the appropriate properties, or another API object of the same type.",
      "signatures": [
        {
          "params": [
            {
              "name": "properties",
              "type": "Interfaces.TextColumnUpdateData",
              "required": true,
              "description": "A JavaScript object with properties that are structured isomorphically to the properties of the object on which the method is called."
            },
            {
              "name": "options",
              "type": "OfficeExtension.UpdateOptions",
              "required": false,
              "description": "Provides an option to suppress errors if the properties object tries to set any read-only properties."
            }
          ],
          "returns": {
            "type": "void",
            "description": null
          }
        },
        {
          "params": [
            {
              "name": "properties",
              "type": "Word.TextColumn",
              "required": true,
              "description": null
            }
          ],
          "returns": {
            "type": "void",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Configure multiple properties of a text column to set its width to 200 points and spacing to 36 points",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first section's text columns\n    const section = context.document.sections.getFirst();\n    const textColumn = section.body.textColumns.getFirst();\n    \n    // Set multiple properties at once\n    textColumn.set({\n        width: 200,\n        spaceAfter: 36\n    });\n    \n    await context.sync();\n    console.log(\"Text column properties updated successfully\");\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "toJSON",
      "kind": "serialize",
      "description": "Overrides the JavaScript toJSON() method in order to provide more useful output when an API object is passed to JSON.stringify(). (JSON.stringify, in turn, calls the toJSON method of the object that's passed to it.) Whereas the original Word.TextColumn object is an API object, the toJSON method returns a plain JavaScript object (typed as Word.Interfaces.TextColumnData) that contains shallow copies of any loaded child properties from the original object.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "Word.Interfaces.TextColumnData",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Serialize a text column's properties to a plain JavaScript object for logging or data transfer purposes.",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first section's text columns\n    const section = context.document.sections.getFirst();\n    const textColumns = section.body.textColumns;\n    \n    // Load the first text column with its properties\n    const firstColumn = textColumns.getFirst();\n    firstColumn.load(\"width,spaceAfter\");\n    \n    await context.sync();\n    \n    // Convert the TextColumn object to a plain JavaScript object\n    const columnData = firstColumn.toJSON();\n    \n    // Now you can use the plain object for logging, storage, or data transfer\n    console.log(\"Column width:\", columnData.width);\n    console.log(\"Space after:\", columnData.spaceAfter);\n    console.log(\"Full column data:\", JSON.stringify(columnData, null, 2));\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "track",
      "kind": "track",
      "description": "Track the object for automatic adjustment based on surrounding changes in the document. This call is a shorthand for [context.trackedObjects.add(thisObject)](/en-us/javascript/api/office/officeextension.clientrequestcontext#office-officeextension-clientrequestcontext-trackedobjects-member). If you're using this object across .sync calls and outside the sequential execution of a \".run\" batch, and get an \"InvalidObjectPath\" error when setting a property or invoking a method on the object, you need to add the object to the tracked object collection when the object was first created. If this object is part of a collection, you should also track the parent collection.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "Word.TextColumn",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Track a text column object to safely access its properties across multiple sync calls when working with a multi-column section layout.",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first section and its text columns\n    const section = context.document.sections.getFirst();\n    const textColumns = section.body.textColumns;\n    textColumns.load(\"items\");\n    \n    await context.sync();\n    \n    // Get the first column and track it for use across sync calls\n    const firstColumn = textColumns.items[0];\n    firstColumn.track();\n    \n    // Load properties\n    firstColumn.load(\"width,spaceAfter\");\n    await context.sync();\n    \n    // Now we can safely use the tracked object after another sync\n    console.log(`Column width: ${firstColumn.width}`);\n    console.log(`Space after: ${firstColumn.spaceAfter}`);\n    \n    // Untrack when done to free up memory\n    firstColumn.untrack();\n    \n    await context.sync();\n});",
          "output_code": null
        }
      ]
    },
    {
      "name": "untrack",
      "kind": "untrack",
      "description": "Release the memory associated with this object, if it has previously been tracked. This call is shorthand for [context.trackedObjects.remove(thisObject)](/en-us/javascript/api/office/officeextension.clientrequestcontext#office-officeextension-clientrequestcontext-trackedobjects-member). Having many tracked objects slows down the host application, so please remember to free any objects you add, once you're done using them. You'll need to call context.sync() before the memory release takes effect.",
      "signatures": [
        {
          "params": [],
          "returns": {
            "type": "Word.TextColumn",
            "description": null
          }
        }
      ],
      "examples": [
        {
          "description": "Access a text column in a section, perform operations on it, then untrack it to free memory after use.",
          "usage_code": "await Word.run(async (context) => {\n    // Get the first section and its text columns\n    const section = context.document.sections.getFirst();\n    const textColumns = section.body.textColumns;\n    const firstColumn = textColumns.getFirst();\n    \n    // Track the column object to work with it\n    firstColumn.track();\n    \n    // Load properties to perform operations\n    firstColumn.load(\"width,spaceAfter\");\n    await context.sync();\n    \n    // Perform operations with the column\n    console.log(`Column width: ${firstColumn.width}`);\n    console.log(`Space after: ${firstColumn.spaceAfter}`);\n    \n    // Untrack the object to release memory after we're done\n    firstColumn.untrack();\n    await context.sync();\n});",
          "output_code": null
        }
      ]
    }
  ],
  "source": {
    "urls": [
      "/en-us/javascript/api/word"
    ]
  }
}